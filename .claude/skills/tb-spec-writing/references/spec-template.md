# spec 템플릿

`docs/superpowers/specs/{YYYY-MM-DD}-{slug}-design.md`의 골격. 해당 없는 절은 지운다 — 빈 껍데기를 남기지 않는다.

```markdown
# {대상} {한 줄 제목}

- 날짜: {YYYY-MM-DD}
- 위치: {core/ | backend/ | frontend/ | 여러 워크스페이스}
- 상태: 초안 (검토 대기)
- 선행 문서: {경로 또는 없음}

## 목적

{무엇을 가능하게 하는가. 사용자·운영자 관점에서 한 문단.}

{핵심 흐름을 한 줄 다이어그램으로:}
```
A → B → C → D
```

## 선행 문서로부터의 변경   ← 선행 문서가 있을 때만

| 항목 | 선행 문서 | 본 문서 |
|------|-----------|---------|

### 왜 {결정}을 뒤집는가

{선행 문서가 든 근거를 하나씩 재평가한다. 뭉뚱그리지 않는다.}

## 결정 사항

| 항목 | 선택 |
|------|------|

## 아키텍처

```
{데이터·제어 흐름 다이어그램. 커밋 경계·중단 지점을 ★ 등으로 표시}
```

{각 구성 요소가 왜 그 자리에 있는지 산문으로 전개}

## 스키마   ← DB를 건드릴 때만

```sql
```

{멱등성·인덱스 조건의 의도를 설명}

## 인터페이스

{모듈별 export 시그니처. 계획이 태스크로 쪼갤 수 있는 단위로.}

```ts
```

## 에러 처리

| 실패 지점 | 상태 변경 | attempt | 루프 |
|---|---|---|---|

### 이미 알려진 함정의 재현

{`.claude/learnings/`에서 이 설계에 해당하는 함정을 인용하고, 이 설계가 어떻게 피하는지 쓴다.}

### 신규 함정

{이 설계가 **새로 만드는** 위험. 없다면 아직 충분히 보지 않은 것이다.}

## CLI / API   ← 외부 인터페이스가 있을 때만

```
{명령 시그니처 또는 엔드포인트}
```

{출력 예시}

## 파일 구조

```
{경로}    # 신규 | 수정 | 무수정
```

## 데이터 흐름

```
{한 건이 시스템을 통과하는 전 과정. 커밋 지점 표시.}
```

## 테스트

{모듈별로 무엇을 검증할지 목록. 계획 작성자가 이 목록을 태스크의 테스트 Step으로 옮긴다.}

**{모듈명}**
- {검증 항목}

## 검증 계획

1. 타입 검사 통과
2. 단위 테스트 전부 통과
3. 빌드 성공
4. {실측 항목}

| 확인 항목 | 판단 기준 |
|---|---|

## 알아둘 트레이드오프

{이 설계가 포기한 것과, 그것이 나중에 어떻게 드러날지}

## 범위 밖 (YAGNI)

- **{항목}** — {왜 지금 하지 않는가}

## 미해결 질문   ← 있을 때만

- {질문}: {선택 A → 귀결} / {선택 B → 귀결}
```

## 절 선택 기준

| 절 | 언제 넣나 |
|---|---|
| 선행 문서로부터의 변경 | 기존 결정을 뒤집을 때만 |
| 스키마 | DB DDL이 바뀔 때만 |
| CLI / API | 외부 인터페이스가 바뀔 때만 |
| 미해결 질문 | 임의로 정하면 안 되는 갈림길이 남았을 때 |

**목적 · 결정 사항 · 아키텍처 · 에러 처리 · 검증 계획 · 트레이드오프 · 범위 밖**은 항상 넣는다.
