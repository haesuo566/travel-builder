# 계획 템플릿

`docs/superpowers/plans/{YYYY-MM-DD}-{slug}.md`의 골격.

````markdown
# {대상} {한 줄 제목} 구현 계획

> **For agentic workers:** 이 계획은 `tb-harness`의 work 단계가 태스크 묶음 단위로 실행한다.
> 각 Step의 체크박스(`- [ ]`)를 완료할 때마다 갱신한다.

**Goal:** {한 문장. 무엇이 가능해지는가.}

**Architecture:** {2~4문장. 왜 이 구조인지, 핵심 안전 장치가 무엇인지.}

**Tech Stack:** {언어·런타임·주요 의존}

**설계 문서:** `docs/superpowers/specs/{YYYY-MM-DD}-{slug}-design.md`

## Global Constraints

- 작업 디렉터리는 `{workspace}/`. 모든 명령은 거기서 실행한다.
- {워크스페이스 고유 제약}
- 주석·로그 메시지·에러 메시지는 **한국어**로 쓴다.
- 테스트는 전부 모킹이다. **실제 네트워크·DB 호출을 하지 않는다.**
- 테스트 실행: `{명령}`. 타입 검사: `{명령}`.
- {절대 하지 않을 것}

---

### Task 1: {제목}

{왜 필요한지 1~2문장}

**Files:**
- Create: `{경로}`      ← 신규일 때
- Modify: `{경로}`      ← 수정일 때
- Test: `{경로}`

**Interfaces:**
- Consumes: {선행 태스크가 만든 것, 또는 "없음"}
- Produces: {이 태스크가 export하는 시그니처}

- [ ] **Step 1: 실패하는 테스트 작성**

{삽입 위치 특정}:

```ts
{테스트 코드 전문}
```

- [ ] **Step 2: 실패를 확인**

```
{테스트 명령}
```

Expected: FAIL — {예상 실패 메시지}

- [ ] **Step 3: 구현**

{삽입 위치 특정}:

```ts
{구현 코드 전문}
```

- [ ] **Step 4: 통과를 확인**

```
{테스트 명령}
{타입 검사 명령}
```

Expected: PASS (기존 테스트 포함 전부)

- [ ] **Step 5: 커밋**

```bash
git add {파일들}
git commit -m "{type}({scope}): {제목}

{왜 이렇게 했는지 2~4줄}"
```

---

### Task 2: ...

---

## 리뷰 묶음

| 묶음 | 태스크 | 논리 단위 |
|---|---|---|
| A | 1~3 | {단위 이름} |

## 최종 검증

- [ ] `{타입 검사 명령}` 통과
- [ ] `{전체 테스트 명령}` 통과
- [ ] `{빌드 명령}` 성공
- [ ] {실측 항목 — 있으면}
````

## 태스크 앞 Step 번호

준비 작업(mock 추가 등)이 필요하면 Step 1 **앞에** 넣고 전체 번호를 밀어 쓴다. 다섯 Step의 **순서**가 규약이지 번호가 규약은 아니다.

```
- [ ] **Step 1: 테스트 mock에 getCollection 추가**
- [ ] **Step 2: 실패하는 테스트 작성**
- [ ] **Step 3: 실패를 확인**
- [ ] **Step 4: 구현**
- [ ] **Step 5: 통과를 확인**
- [ ] **Step 6: 커밋**
```

## 커밋 메시지 규약

```
{type}({scope}): {한국어 제목 — 무엇을}

{왜 이렇게 했는지. diff가 말하지 않는 것만.}
```

- `type`: `feat` `fix` `refactor` `test` `docs` `chore`
- `scope`: `core` `backend` `frontend` (여러 워크스페이스에 걸치면 생략)
- 제목은 명령형·한국어. 본문은 **왜**만 쓴다 — 무엇을 했는지는 diff가 말한다.
